# Required for live SEC EDGAR requests. Tests do not need this.
# SEC fair-access policy requires an identifying User-Agent with contact email.
EQUITYTRACE_SEC_EMAIL=

# Optional organization name used in the User-Agent string.
EQUITYTRACE_SEC_ORGANIZATION=EquityTrace Development

# DuckDB database path (created on init-db / first ingest).
EQUITYTRACE_DATABASE_PATH=data/equitytrace.duckdb

# Optional HTTP response cache directory.
EQUITYTRACE_CACHE_DIR=data/cache

# Request timeout in seconds.
EQUITYTRACE_HTTP_TIMEOUT_SECONDS=30

# Max requests per second (must stay below 10 per SEC guidance).
EQUITYTRACE_MAX_REQUESTS_PER_SECOND=8

# Legacy FILINGEDGE_* variables are still accepted temporarily when the matching
# EQUITYTRACE_* variable is unset. Prefer the names above.
